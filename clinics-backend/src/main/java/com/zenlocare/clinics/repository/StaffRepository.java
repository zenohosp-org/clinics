package com.zenlocare.clinics.repository;

/**
 * @deprecated Use {@link UserRepository} instead. Kept empty to avoid broken
 *             file references.
 */
@Deprecated(forRemoval = true)
interface StaffRepository {
    // Empty — replaced by UserRepository
}
